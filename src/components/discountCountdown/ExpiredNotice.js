import React from "react";

const DateTimeDisplay = () => {
  // const dateTime = Intl.NumberFormat("en-EN").format(value);

  return (
    <div>
      {" "}
      <p className="text-md ">expireDiscount</p>
    </div>
  );
};

export default DateTimeDisplay;
